# ملخص نظام الاشتراكات — شرارة ⚡
تاريخ: 2026-08-08

## الخلاصة
نظام أكواد التفعيل تحوّل من "فتح دائم بكود واحد" إلى **اشتراكات شهرية/سنوية** فعلية بتاريخ انتهاء، بالإضافة لصفحة أسعار جديدة تُعرض للطلاب قبل ما يتواصلون عبر واتساب.

---

## كيف يشتغل النظام الآن

**الأسعار الحالية:** شهري 5,000 د.ع · سنوي 30,000 د.ع (يوفّر ~50%) — مكتوبة بـ [subscribe.html](subscribe.html)، عدّلها هناك إذا تغيّرت.

**عند تفعيل كود جديد:** الأستاذ يختار من قائمة "أكواد شرارة" بالشيت ▸ "توليد كود جديد" — يسأل عن اسم الطالب، طريقة الدفع، ونوع الاشتراك (شهري/سنوي). الكود يُنشأ بحالة "غير مستخدم"، وتاريخ الانتهاء يُحسب تلقائياً أول ما الطالب يفعّله فعلياً (شهري = +30 يوم، سنوي = +سنة من تاريخ التفعيل).

**عند انتهاء الاشتراك:** الموقع يعيد قفل نفسه تلقائياً (يتحقق محلياً من تاريخ الانتهاء بلا حاجة لطلب شبكة طالما لسا ساري)، ويظهر للطالب زر "شوف خطط الاشتراك" بدل الفتح المباشر.

**لتجديد كود موجود:** الأستاذ يستخدم "تجديد كود" من نفس قائمة الشيت — يدخل الكود، يختار النوع (شهري/سنوي، ممكن يغيّره عن الأول)، وتاريخ الانتهاء يتمدّد تلقائياً.

**الأكواد القديمة** (المفعّلة قبل هذا التحديث، 2026-08-08): مُنحت فترة سماح تنتهي **2027-08-08** بالضبط لكل الجميع (مو حسب تاريخ تفعيل كل وحدة).

**صفحة الأسعار** ([subscribe.html](subscribe.html)): تُعرض للطالب لما يضغط "شوف خطط الاشتراك" بشاشة القفل، وفيها رابط بالصفحة الرئيسية أيضاً. كل بطاقة سعر فيها زر واتساب يفتح برسالة جاهزة تذكر الخطة بالضبط.

---

## الملفات المتأثرة

- [js/access-control.js](js/access-control.js) — يتحقق من تاريخ الانتهاء محلياً، يميّز رسالة "منتهي" عن "غير مفعّل أصلاً"، ويربط زر واتساب بصفحة الأسعار
- [subscribe.html](subscribe.html) — صفحة الأسعار الجديدة
- [css/style.css](css/style.css) — أنماط بطاقات الأسعار (`.pricing-grid`, `.plan-card`)
- [index.html](index.html) — زر "💳 الاشتراك" جديد بالهيدر
- **Google Apps Script** (خارج هذا المستودع، بمحرر script.google.com المربوط بشيت "الأكواد") — أُعيد كتابته بالكامل: يحسب/يتحقق من تاريخ الانتهاء حسب النوع، دالتا `generateNewCode`/`renewCode` تسألان عن النوع، ودالة `migrateLegacyCodes` (تُشغَّل يدوياً، مرة وحدة، من محرر السكربت مباشرة) رحّلت كل الأكواد القديمة لتاريخ السماح الموحّد

## بنية شيت "الأكواد" (أعمدة جديدة)
A=الكود · B=الحالة · C=معرّف الجهاز · D=اسم الطالب · E=طريقة الدفع · F=تاريخ التفعيل · **G=نوع الاشتراك (جديد)** · **H=تاريخ الانتهاء (جديد)**

---

## أشياء يلزم تذكرها

- تاريخ فترة السماح (2027-08-08) مكرّر بمكانين يدوياً — `LEGACY_EXPIRY_ISO` بـ access-control.js و`LEGACY_EXPIRY` بالـ Apps Script — إذا تغيّر لازم تعدّل الاثنين معاً.
- فحص الانتهاء بالمتصفح يعتمد جزئياً على ساعة الجهاز المحلي (للسرعة، بلا طلب شبكة كل مرة) — مو حاجز أمان حقيقي، بس السيرفر هو المرجع الفعلي عند أي تفعيل جديد.
- أي تعديل مستقبلي على كود الـ Apps Script لازم يُنشر بخطوة منفصلة: Deploy ▸ Manage deployments ▸ ✏️ ▸ New version ▸ Deploy — تعديل الكود وحفظه لحاله ما يكفي.

## الحالة الحالية
✅ منشور ومُتحقَّق منه بالكامل (Apps Script Version 7، طرف العميل مُختبر بمتصفح تجريبي، صفحة الأسعار شغّالة).
