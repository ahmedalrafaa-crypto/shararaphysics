# ملخص نظام الاشتراكات — شرارة ⚡
تاريخ: 2026-08-08

## الخلاصة
نظام أكواد التفعيل تحوّل من "فتح دائم بكود واحد" إلى **اشتراكات شهرية/سنوية** فعلية بتاريخ انتهاء، بالإضافة لصفحة أسعار جديدة تُعرض للطلاب قبل ما يتواصلون عبر واتساب.

---

## كيف يشتغل النظام الآن

**الأسعار الحالية (فُصلت حسب المرحلة بتاريخ 2026-08-27):**

| المرحلة | شهري | سنوي |
|---|---|---|
| الثالث المتوسط | 5,000 د.ع | 30,000 د.ع |
| السادس العلمي | 8,000 د.ع | 45,000 د.ع |
| الخامس العلمي | **مجاني بالكامل** — بلا كود ولا اشتراك | — |

الأسعار مكتوبة بمكانين لازم يتطابقان: [subscribe.html](subscribe.html) (واجهة الطالب) وثابت `STAGES` بـ[admin.html](admin.html) (تسميات أزرار التوليد).

**كل كود صار له مرحلة** (`g3` / `g6` / `all`): كود الثالث المتوسط يفتح `chapters/chN/` وتوقعات الثالث فقط، وكود السادس يفتح `chapters/g6cN/` وتوقعات السادس والأسئلة الغريبة فقط. أدوات مشتركة (البحث، التحليل) يفتحها أي اشتراك فعّال. **الأكواد القديمة (قبل هذا التغيير) عمود مرحلتها فارغ = `all`، فتبقى شغّالة على المرحلتين تماماً كما كانت** — السعر الجديد للمشتركين الجدد فقط.

**عند تفعيل كود جديد:** الأستاذ يختار من قائمة "أكواد شرارة" بالشيت ▸ "توليد كود جديد" — يسأل عن اسم الطالب، طريقة الدفع، نوع الاشتراك (شهري/سنوي)، **والمرحلة (3 / 6 / 0 للمرحلتين)**. أو من [admin.html](admin.html) بالموبايل مباشرة (فيه قائمة منسدلة للمرحلة، وأسعار الأزرار تتبدّل حسبها). الكود يُنشأ بحالة "غير مستخدم"، وتاريخ الانتهاء يُحسب تلقائياً أول ما الطالب يفعّله فعلياً (شهري = +30 يوم، سنوي = +سنة من تاريخ التفعيل).

**عند انتهاء الاشتراك:** الموقع يعيد قفل نفسه تلقائياً (يتحقق محلياً من تاريخ الانتهاء بلا حاجة لطلب شبكة طالما لسا ساري)، ويظهر للطالب زر "شوف خطط الاشتراك" بدل الفتح المباشر.

**لتجديد كود موجود:** الأستاذ يستخدم "تجديد كود" من نفس قائمة الشيت — يدخل الكود، يختار النوع (شهري/سنوي، ممكن يغيّره عن الأول)، **واختيارياً ينقله لمرحلة ثانية** (اتركه فارغاً = تبقى مرحلته)، وتاريخ الانتهاء يتمدّد تلقائياً. نفس الشي متاح بـ[admin.html](admin.html).

**الأكواد القديمة** (المفعّلة قبل هذا التحديث، 2026-08-08): مُنحت فترة سماح تنتهي **2027-08-08** بالضبط لكل الجميع (مو حسب تاريخ تفعيل كل وحدة).

**صفحة الأسعار** ([subscribe.html](subscribe.html)): تُعرض للطالب لما يضغط "شوف خطط الاشتراك" بشاشة القفل، وفيها رابط بالصفحة الرئيسية أيضاً. كل بطاقة سعر فيها زر واتساب يفتح برسالة جاهزة تذكر الخطة بالضبط.

---

## الملفات المتأثرة

- [js/access-control.js](js/access-control.js) — يتحقق من تاريخ الانتهاء محلياً، يميّز رسالة "منتهي" عن "غير مفعّل أصلاً"، ويربط زر واتساب بصفحة الأسعار
- [subscribe.html](subscribe.html) — صفحة الأسعار الجديدة
- [css/style.css](css/style.css) — أنماط بطاقات الأسعار (`.pricing-grid`, `.plan-card`)
- [index.html](index.html) — زر "💳 الاشتراك" جديد بالهيدر
- **Google Apps Script** (خارج هذا المستودع، بمحرر script.google.com المربوط بشيت "الأكواد") — أُعيد كتابته بالكامل: يحسب/يتحقق من تاريخ الانتهاء حسب النوع، دالتا `generateNewCode`/`renewCode` تسألان عن النوع، ودالة `migrateLegacyCodes` (تُشغَّل يدوياً، مرة وحدة، من محرر السكربت مباشرة) رحّلت كل الأكواد القديمة لتاريخ السماح الموحّد

## بنية شيت "الأكواد"
A=الكود · B=الحالة · C=معرّف الجهاز · D=اسم الطالب · E=طريقة الدفع · F=تاريخ التفعيل · G=نوع الاشتراك · H=تاريخ الانتهاء · **I=المرحلة (جديد 2026-08-27)**

عمود I يقبل `g3` أو `g6` أو `all`. **الفراغ = `all`** (وهذا اللي يحمي المشتركين القدام). ضع بالخلية I1 عنواناً "المرحلة" للوضوح — الكود يشتغل بدونه.

---

## أشياء يلزم تذكرها

- **فصل المراحل يفشل مفتوحاً عمداً:** لو الـApps Script لسا ما انتشرت نسخته الجديدة، السيرفر ما يرجّع مرحلة إطلاقاً، وحينها `access-control.js` **ما يقفل أحداً** بدل ما يقفل الكل. يعني ترتيب النشر (الموقع أولاً أو السكربت أولاً) ما يضرّ الطلاب — أسوأ حالة إن الفصل ما يشتغل لحد ما تنشر السكربت.
- الأسعار مكتوبة بمكانين (subscribe.html و`STAGES` بـadmin.html) — لو غيّرتها غيّرها بالاثنين.
- تاريخ فترة السماح (2027-08-08) مكرّر بمكانين يدوياً — `LEGACY_EXPIRY_ISO` بـ access-control.js و`LEGACY_EXPIRY` بالـ Apps Script — إذا تغيّر لازم تعدّل الاثنين معاً.
- فحص الانتهاء بالمتصفح يعتمد جزئياً على ساعة الجهاز المحلي (للسرعة، بلا طلب شبكة كل مرة) — مو حاجز أمان حقيقي، بس السيرفر هو المرجع الفعلي عند أي تفعيل جديد.
- أي تعديل مستقبلي على كود الـ Apps Script لازم يُنشر بخطوة منفصلة: Deploy ▸ Manage deployments ▸ ✏️ ▸ New version ▸ Deploy — تعديل الكود وحفظه لحاله ما يكفي.

## نشر تحديثات الموقع (تحديث 2026-08-09)
الموقع صار مربوط بـ [GitHub](https://github.com/ahmedalrafaa-crypto/shararaphysics) + Netlify (Continuous Deployment) — أي `git push` لفرع `main` ينشر نفسه تلقائياً على [shararaphysics.netlify.app](https://shararaphysics.netlify.app)، بلا حاجة لسحب المجلد يدوياً بعد الآن.

## الحالة الحالية
✅ نظام الاشتراكات الأساسي منشور ومُتحقَّق منه (Apps Script Version 7، صفحة الأسعار شغّالة، النشر التلقائي عبر GitHub مفعّل).

🔸 **فصل المراحل (2026-08-27):** طرف الموقع جاهز ومُختبَر محلياً (٢٥ فحص منطق + اختبار حيّ بالمتصفح لكل السيناريوهات). **الناقص: لصق ونشر نسخة الـApps Script الجديدة** — لين ما تُنشر، الموقع يشتغل بلا فصل مراحل (كل كود يفتح المرحلتين، مثل الآن بالضبط) ولا ينقفل أي طالب.
